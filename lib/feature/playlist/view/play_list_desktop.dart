import 'package:flutter/material.dart';

class PlayListDesktop extends StatelessWidget {
  const PlayListDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
